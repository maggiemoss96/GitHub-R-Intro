---
title: "Moss_MapChallenge"
author: "Maggie Moss"
date: "2025-11-12"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r setup}
source("setup.R")

#install packages
install.packages("gifski")
library(gifski)

#load in geologic map shapefile - state geologic map compilation dataset
sgmc <- "https://www.sciencebase.gov/catalog/file/get/5888bf4fe4b05ccb964bab9d?f=__disk__24%2Ff6%2Fe1%2F24f6e139c181fd9fe43df2aaf7f50b1c5b3b6297/USGS_SGMC_Shapefiles.zip"

download.file(sgmc, destfile = "SGMC_shapefiles.zip", mode = "wb")
unzip("SGMC_shapefiles.zip", exdir = "SGMC_shapefiles")

```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
